pre-commit:
  parallel: true
  commands:
    gofmt:
      glob: "*.go"
      run: test -z "$(gofmt -l {staged_files})"
    go-test:
      run: go test ./...

pre-push:
  parallel: true
  commands:
    go:
      run: go test ./...
    flake:
      run: nix flake check --no-build
