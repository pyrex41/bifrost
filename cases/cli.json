{
  "group": "cli",
  "cases": [
    {
      "name": "eval-prints-value",
      "mode": "eval",
      "expr": "(+ 40 2)",
      "expect": "output",
      "golden": "42",
      "doc": "CLI parity: 'eval -e EXPR' prints the value of EXPR to stdout"
    },
    {
      "name": "version-expr-42",
      "mode": "eval",
      "expr": "(version)",
      "expect": "output",
      "golden": "42",
      "doc": "CLI parity: (version) returns runtime kernel version 42 uniformly (works on every impl, incl. shen-lua which has no --version flag)"
    },
    {
      "name": "version-banner-contains",
      "mode": "version",
      "expect": "version",
      "contains": "42",
      "doc": "CLI parity: the --version banner contains runtime kernel version 42. shen-lua has no --version flag, so Bifrost falls back to eval of (version). Experimental shen-c/shen-forth banners are port-local; this case uses kernels/adapters that emit 42."
    },
    {
      "name": "stdin-eof-clean-exit",
      "mode": "repl-eof",
      "expect": "clean-exit",
      "doc": "DELIBERATE IMPROVEMENT OVER CANONICAL (not a spec conformance): starting the REPL and closing stdin (EOF) exits cleanly on every maintained impl. Canonical Shen LOOPS FOREVER here — the kernel shen.loop catches the reader's EOF error, prints it, and recurses (e.g. official shen-cl emits ~26MB of 'NIL is not of type CHARACTER' in 8s). All ports here detect EOF at the input layer and exit 0; this case guards that they keep doing so. (Earlier this passed spuriously for shen-cl because its repl adapter used a bare -q, which shen-cl rejects as an invalid arg and exits before the REPL even starts — now fixed to bare {bin}.)"
    }
  ]
}
